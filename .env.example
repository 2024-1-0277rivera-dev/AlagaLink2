# Do NOT commit real secrets to source control.
# Copy this file to `.env.local` and add your real keys.

# Server-side (do not expose to client)
GROQ_API_KEY=your_groq_api_key_here

# Public key (only for client-side demos; avoid in production)
NEXT_PUBLIC_GROQ_API_KEY=optional_public_key_here
